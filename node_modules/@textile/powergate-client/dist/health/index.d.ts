import { CheckResponse } from "@textile/grpc-powergate-client/dist/health/rpc/rpc_pb";
import { Config } from "../types";
export interface Health {
    /**
     * Checks the Powergate node health.
     * @returns Information about the health of the Powergate node.
     */
    check: () => Promise<CheckResponse.AsObject>;
}
/**
 * @ignore
 */
export declare const createHealth: (config: Config) => Health;
